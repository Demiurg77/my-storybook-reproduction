import { Button } from "./Button";

export const ButtonExternalCSF2 = () => (
  <div>
    <Button primary label="Button in CSF 2 and in external file" />
    <p style={{ color: "red" }}>"Show code" is disabled.</p>
  </div>
);
